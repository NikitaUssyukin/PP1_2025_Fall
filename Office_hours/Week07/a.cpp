#include <iostream>

using namespace std;

int main() {

    int a, b;
    a = 3;     // assign 3 into a
    b = 2;     // assign 2 into b
    a = b;     // assign a into b (assign the value of a into b)
    cout << a; // a is ?
    cout << endl;
    cout << a + 2 + 3; // a is ?

    return 0;
}